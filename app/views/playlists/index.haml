%h2 All Playlists: 

%ul.playlists
	-@playlists.each do |playlist|
		%li= link_to playlist.name, playlist_path(playlist.id)

.actions
	= link_to "Create A New Playlist", new_playlist_path, class: 'button'
	= link_to "Search", "/search", class: 'button'
